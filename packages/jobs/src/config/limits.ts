import { env } from "./env";

export const MAX_CODE_SIZE_BYTES = env.CODE_MAX_SIZE_BYTES;
export const MAX_STDIN_SIZE_BYTES = env.STDIN_MAX_SIZE_BYTES;
export const MAX_STDOUT_SIZE_BYTES = env.STDOUT_MAX_SIZE_BYTES;
